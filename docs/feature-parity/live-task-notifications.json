{
  "$schema": "../schemas/feature-parity.schema.json",
  "schemaVersion": 1,
  "feature": "live-task-notifications",
  "summary": "Claude's live system/task_notification (a backgrounded Bash command or subagent settled) becomes a task.notification runtime event, so the compact 'Background task failed: ...' row shows as it happens instead of only after a reload. Every client builds the row from the same fields the transcript's <task-notification> line carries (status, summary, task id, output file) and splits it with the existing synthetic-message code, so the live row and the reloaded row are the same row. The registry also stores the live event once per task, and a reload pairs it with the transcript line (see persisted-task-notices). The two can share no id (the live event carries the SDK message uuid, the transcript row its own line uuid), so a live notice that arrives after a reload or re-seed (resume replay, Android's buffered events replayed by installSnapshot) is dropped when a transcript row already shows the same task id, status and summary (trimmed on both sides, as the transcript parser does) stamped within 5s of it either way (transcriptShowsTaskNotification, ported to Kotlin).",
  "surfaces": {
    "desktop": {
      "status": "implemented",
      "evidence": [
        "src/renderer/components/chat/provider-event-reducer.ts",
        "src/renderer/components/chat/ChatPanel.tsx"
      ]
    },
    "reactNativeIos": {
      "status": "implemented",
      "evidence": [
        "apps/mobile/src/stores/chat.ts",
        "apps/mobile/src/lib/thread-history.ts"
      ]
    },
    "nativeAndroid": {
      "status": "implemented",
      "evidence": [
        "apps/android/app/src/main/java/app/switchboard/mobile/domain/thread/ThreadEventDecoder.kt",
        "apps/android/app/src/main/java/app/switchboard/mobile/domain/thread/ThreadModels.kt",
        "apps/android/app/src/main/java/app/switchboard/mobile/domain/thread/SyntheticUserMessage.kt",
        "apps/android/app/src/main/java/app/switchboard/mobile/data/thread/ThreadStoreReducer.kt",
        "apps/android/app/src/main/java/app/switchboard/mobile/protocol/WsProtocol.kt",
        "apps/android/app/src/test/java/app/switchboard/mobile/data/thread/ThreadStoreReducerTest.kt"
      ]
    },
    "sharedBackendApi": {
      "status": "implemented",
      "evidence": [
        "src/shared/provider-events.ts",
        "src/shared/synthetic-message.ts",
        "src/main/provider/adapters/claude-adapter.ts",
        "tests/unit/live-task-notification.test.ts"
      ]
    },
    "storageDataMigration": {
      "status": "not_applicable",
      "reason": "Nothing new in this change. Storing the live notice is persisted-task-notices; the transcript's <task-notification> user line was already stored and rendered on reload."
    },
    "updateRelease": {
      "status": "implemented",
      "evidence": [
        "CHANGELOG.md"
      ]
    }
  },
  "verification": {
    "automated": [
      "npm run typecheck",
      "npm test -- --maxWorkers=2",
      "npm test --prefix apps/mobile and cd apps/mobile && npx tsc --noEmit",
      "cd apps/android && ./gradlew testDebugUnitTest"
    ],
    "hardware": [],
    "notExercised": [
      "No live Claude session was run with a failing background task; the adapter test uses the SDK's SDKTaskNotificationMessage shape and the transcript line recorded on this machine.",
      "Clients older than this release ignore task.notification (desktop and the React Native app drop unknown events), except native Android before this release, which shows it as an 'Unsupported runtime event' notice until updated. The row still appears after a reload on all of them.",
      "Codex has no live equivalent: app-server's only exit notification, process/exited, answers a client-issued process/spawn, which Switchboard never sends. Its synthetic transcript blocks are context bootstraps and <turn_aborted>, and the interrupt already shows live through turn/completed."
    ]
  }
}
