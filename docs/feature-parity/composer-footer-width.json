{
  "$schema": "../schemas/feature-parity.schema.json",
  "schemaVersion": 1,
  "feature": "composer-footer-width",
  "summary": "Keep the desktop composer footer on one row: short runtime-mode labels with the detail as a tooltip, no key-hint text (the send button's tooltip names Enter, Shift+Enter and the follow-up keys), and Alt+Enter that still queues while an autocomplete menu is open.",
  "surfaces": {
    "desktop": {
      "status": "implemented",
      "evidence": [
        "src/renderer/components/chat/ChatInput.tsx"
      ]
    },
    "reactNativeIos": {
      "status": "not_applicable",
      "reason": "apps/mobile has no runtime-mode select or desktop footer hint; its composer layout is separate and unchanged."
    },
    "nativeAndroid": {
      "status": "not_applicable",
      "reason": "The Compose composer has no runtime-mode select or desktop footer hint; nothing it renders is affected."
    },
    "sharedBackendApi": {
      "status": "implemented",
      "evidence": [
        "src/shared/turn-delivery.ts",
        "tests/unit/turn-delivery.test.ts"
      ]
    },
    "storageDataMigration": {
      "status": "not_applicable",
      "reason": "No stored data changes. The runtime-mode values persisted per conversation are the same five values."
    },
    "updateRelease": {
      "status": "not_applicable",
      "reason": "An unconditional desktop renderer fix with no flag, package identity, signing, update-channel or rollout change."
    }
  },
  "verification": {
    "automated": [
      "npx vitest run tests/unit/turn-delivery.test.ts",
      "npm run typecheck",
      "npm test"
    ],
    "hardware": [],
    "notExercised": [
      "The footer at narrow pane widths and the Alt+Enter path with an open @ or slash menu were not exercised in a running app."
    ]
  }
}
