{
  "$schema": "../schemas/feature-parity.schema.json",
  "schemaVersion": 1,
  "feature": "file-diff-cards-opt-in",
  "summary": "Make the in-chat per-file diff cards (with per-hunk accept/reject) opt-in and off by default, since most users only review a diff once it becomes a PR. Off collapses a turn's changed files to a single \"Changed N files\" button that expands the real cards for that turn only; the backend keeps computing and emitting file.edited unconditionally.",
  "surfaces": {
    "desktop": {
      "status": "implemented",
      "evidence": [
        "src/renderer/stores/layout-store.ts",
        "src/renderer/components/chat/MessageList.tsx",
        "src/renderer/components/chat/turnPresentation.ts",
        "src/renderer/components/SettingsModal.tsx",
        "src/renderer/styles/global.css",
        "tests/unit/layout-store.test.ts",
        "tests/unit/message-list.test.ts"
      ]
    },
    "reactNativeIos": {
      "status": "staged",
      "flag": "mobile_file_diff_collapse",
      "followUpRelease": "The React Native thread feed (apps/mobile/src/screens/ThreadScreen.tsx, FileEditItem) already renders one compact, ungrouped one-line +/- summary per changed file with no per-hunk accept/reject, so today's verbosity problem does not exist there the way it does on desktop. Grouping consecutive fileEdit feed items by turn and collapsing them behind a button mirrors a flat FeedItem[] (apps/mobile/src/stores/chat.ts) that has no turn concept at all today - the FlatList renders items individually with no equivalent to groupIntoTurns/projectTurnPresentation. That's new grouping logic, not a render-only toggle, so it ships as its own follow-up mobile release once the feed gains turn grouping, behind the local (per-device, AsyncStorage-backed) mobile_file_diff_collapse flag rather than reading the desktop's chat.showFileDiffs setting over the wire."
    },
    "nativeAndroid": {
      "status": "staged",
      "flag": "mobile_file_diff_collapse",
      "followUpRelease": "Same situation as reactNativeIos: ThreadPresentation.kt's FeedItem.FileEdit renders one compact row per changed file with no per-hunk controls, and there is no turn-grouping concept in the Android feed to collapse. Ships alongside the React Native change, once the Android thread feed gains the same turn-grouping, behind the same named mobile_file_diff_collapse flag (a local Android preference, not the desktop settings key)."
    },
    "sharedBackendApi": {
      "status": "not_applicable",
      "reason": "Render-only change on the desktop client. The git-checkpoint diff computation and the file.edited event (src/main/git/checkpoint.ts, src/shared/provider-events.ts) are unconditional and untouched - every provider still emits one event per changed file per turn regardless of this setting, so mobile/Android and any future consumer keep receiving the same wire data they always did."
    },
    "storageDataMigration": {
      "status": "not_applicable",
      "reason": "No schema change and no migration. The preference is one new row, chat.showFileDiffs, in the existing generic key-value settings table (src/main/db/database.ts). An absent key already means 'no preference' and the reader (hydrateSidebarCollapse in layout-store.ts) treats anything other than the literal string 'true' as off, so an older database (no row) and a newer database (row present) both resolve correctly with no backfill, and an older client that has never heard of the key simply never reads or writes it."
    },
    "updateRelease": {
      "status": "not_applicable",
      "reason": "Renderer presentation change only. No package identity, signing, deep-link scheme, update channel, or wire-protocol change; the file.edited IPC/event contract is untouched."
    }
  },
  "verification": {
    "automated": [
      "npm run typecheck passed for both the main and renderer projects.",
      "npm test passed the full vitest suite including two new tests: layout-store's 'file diff cards opt-in' describe block (default-off, setShowFileDiffCards toggles the flag) and message-list's changedFilesLabel formatting test ('Changed 1 file' / 'Changed 8 files').",
      "npm test --prefix apps/mobile passed unchanged - no mobile files were touched by this change.",
      "Android unit tests (./gradlew testDebugUnitTest) were not run - no apps/android files were touched by this change."
    ],
    "hardware": [
      "Desktop manual verification on this Mac: built with npm run build:fast, launched the app via videos/capture-tour.mjs's own scripted demo fixture (SB_DEMO_ADAPTER=1) with the seeded settings row set to 'false', ran the 'diff-review' scene (a real turn that edits a file) and confirmed the turn collapses to a 'Changed 1 file' button with no per-hunk controls visible. Re-seeded the row to 'true' and reran the same scene, confirming the cards render exactly as before (header + Keep all/Reject all + hunks), matching the pre-existing tour clip. Separately launched a fresh Electron instance with an isolated --user-data-dir and screenshotted Settings > General > Responses, confirming the new 'Show file diff cards in chat' checkbox renders unchecked by default with its description."
    ],
    "notExercised": [
      "The 'Changed N files' button's hover/focus-visible styling and the grid-row expand transition (turn-files-toggle in global.css) were confirmed once in the running app but not across all three themes (dark/light/translucent).",
      "No live remote-backend (WsHost/SSH-tunnelled) session was used to confirm the setting behaves the same when a session is served over the wire instead of the local Electron IPC host - the setting is read through the same window.api.settings surface either way, so no divergence is expected, but it was not exercised."
    ]
  }
}
